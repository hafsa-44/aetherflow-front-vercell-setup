# Rename this file to .env in your project root (next to package.json)
# and replace the value below with your real OpenRouter API key.
# NEVER commit the real key to source control.
VITE_OPENROUTER_API_KEY=your_key_here
